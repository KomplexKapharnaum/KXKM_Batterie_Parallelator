menu "BMU MQTT"
    config BMU_MQTT_BROKER_URI
        string "MQTT Broker URI"
        default "mqtt://kxkm-ai:1883"
        help
            Full URI including protocol. Use mqtts:// for TLS.
    config BMU_MQTT_CLIENT_ID
        string "MQTT Client ID"
        default "kxkm-bmu-01"
    config BMU_MQTT_USERNAME
        string "MQTT Username"
        default ""
    config BMU_MQTT_PASSWORD
        string "MQTT Password"
        default ""
    config BMU_MQTT_RECONNECT_MS
        int "Reconnect delay (ms)"
        default 5000
endmenu
