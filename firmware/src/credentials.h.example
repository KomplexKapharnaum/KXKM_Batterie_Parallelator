#pragma once
// credentials.h — private, do NOT commit to git.
// Copy this file to credentials.h and fill in your values.

// InfluxDB
const char *influxDBServerUrl = "https://us-east-1-1.aws.cloud2.influxdata.com";
const char *influxDBOrg = "YOUR_ORG_ID";
const char *influxDBBucket = "BATT_PARALLELATOR";
const char *influxDBToken = "YOUR_INFLUXDB_TOKEN";
// TLS MUST be enabled for cloud communication — never set to true in production
const bool influxDBInsecure = false;

// MQTT telemetry (firmware -> broker)
// Example with local broker/bridge running on kxkm-ai
const char *mqttBrokerHost = "192.168.1.10";
const uint16_t mqttBrokerPort = 1883;
const char *mqttClientId = "kxkm-bmu-01";
const char *mqttUsername = "";
const char *mqttPassword = "";
const char *mqttTopicTelemetry = "kxkm/bmu/telemetry";

// Azure IoT bridge (consumed by server-side bridge process)
// Keep these values server-side whenever possible.
const char *azureIoTHubHost = "YOUR_HUB.azure-devices.net";
const char *azureDeviceId = "YOUR_DEVICE_ID";
const char *azureDevicePrimaryKey = "YOUR_DEVICE_PRIMARY_KEY";

// Grafana (optional)
// const char *grafanaToken = "YOUR_GRAFANA_TOKEN";
